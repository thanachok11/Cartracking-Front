import React from 'react';

export default function ReportPage() {
	return (
		<div style={{ padding: 20 }}>
			<h2>Report — Events</h2>
			<div>Temporary placeholder for Report page. The rich Report UI exists in earlier edits; please re-open that file if you need the full page restored.</div>
		</div>
	);
}
